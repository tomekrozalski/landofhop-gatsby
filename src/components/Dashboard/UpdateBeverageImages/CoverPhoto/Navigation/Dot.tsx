import styled from 'styled-components';

const Dot = styled.i`
	display: inline-block;
	width: 1rem;
	height: 1rem;
	border-radius: 50%;
	background-color: var(--color-darker);
`;

export default Dot;
